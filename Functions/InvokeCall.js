// In JavaScript, we can divide up our code into reusable parts called functions.

// Here's an example of a function:

function functionName() {
  console.log("Hello World");
}

// You can call or invoke this function by 
// using its name followed by parentheses, like this: 

functionName(); 

//Each time the function is called it will print out the 
// message "Hello World" on the dev console. All of the code between the curly braces 
// will be executed every time the function is called.

// Create a function called reusableFunction which prints "Hi World" to the dev console.
// Call the function.

function reusableFunction() {
    console.log("Hi World")
}
reusableFunction();      //on this line we are calling or invoking the function

// or 
console.log(reusableFunction());

function reusableFunction() {
    console.log('Hi World');
}
reusableFunction();

function reusableFunction() {
    console.log('Hello World');
}
reusableFunction();   //calling the function

function reusableFunction() {
    console.log('Hello World')
}
reusableFunction();    //calling the function


